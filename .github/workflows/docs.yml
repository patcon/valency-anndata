name: Build and deploy documentation

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      UV_PYTHON: "3.11"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          # Works on 3.10 locally, but pacmap seems to crash python kernel on
          # CI. This happens when nbconvert is executing notebooks during its
          # markdown conversion for docs website.
          # TODO: Debug pacmap on github actions, and maybe submit PR upstream.
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install dependencies
        run: |
          make install-all

      - name: Build notebook-derived docs
        continue-on-error: true
        id: build-docs
        run: |
          make notebook-docs

      - name: Run script-ified notebooks on fail
        if: steps.build-docs.outcome == 'failure'
        run: |
          make notebook-docs-debug

      - name: Build MkDocs site
        run: |
          make docs

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Upload site as PR artifact
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: docs-site
          path: site
          retention-days: 7

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
